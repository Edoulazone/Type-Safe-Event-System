{
  "name": "type-safe-event-system",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node dist/tests/test-observables.js",
    "test:ts": "ts-node --esm src/tests/test-observables.ts",
    "dev": "npm run build && node dist/examples/demo.js",
    "validate": "npm run build && node dist/tests/validate-system.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  }
}